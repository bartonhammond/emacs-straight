  Accounts.onResetPasswordLink(function(token, done) {
    console.log('onResetPasswordLike.done: ' + done);
    Session.set('resetPasswordToken', token);  // pull token and place in a session variable, so it can be accessed later 
  });
